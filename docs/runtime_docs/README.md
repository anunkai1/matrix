## Runtime Docs

This directory stores runtime-local documentation that was previously kept only inside service user home directories.

Purpose:
- keep operational docs under version control
- let runtime roots point to repo-backed copies
- avoid losing local-only instructions, summaries, and handoff notes

Files here may still be consumed through symlinks from the live runtime roots.
