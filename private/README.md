# Private Workspace

Use this folder for local personal files that should not be pushed to GitHub.

Rules:
- Keep sensitive files under `private/` (for example: `private/profile.md`).
- Only `private/README.md` and `private/.gitkeep` are tracked.
- Do not store secrets in tracked files.
