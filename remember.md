1. pytest is under .venv/server3-qa/bin/pytest.
2. ARR stack moved to server2 (2026-06-01). Sonarr/Radarr/Prowlarr/qBittorrent/Jellyfin/Jellyseerr all run there. Server3 has none. Server2 Sonarr API: http://192.168.0.118:8989 key=b57a1c1e420d426780164ba002dfd820.
3. TTS capable: use ops/telegram-voice/tts_english.sh to generate OGG voice notes (Microsoft Edge TTS, JennyNeural voice, 1.35x speed). Deliver via Telegram bridge with inline directive: [[audio_as_voice]] [[media: /path/to/file.ogg]].
4. **staker2 = server2 = server 2 = same Intel NUC box (Ubuntu 24.04, LAN 192.168.0.118)**. Reachable from server3 as `architect` via the existing `~/.ssh/config` alias `server2` (port 4720, key `~/.ssh/server2_architect_ed25519`). The Tailscale hostname `staker2.tailfc147.ts.net` (100.112.143.69) resolves but port 22 is **refused** — always go through the `server2` alias. Architect has passwordless sudo on staker2.
5. **meshift** is staker2's Telegram bridge (systemd unit `meshift.service`, running as `lepton`, code in `/home/lepton/meshift/`, config in `/home/lepton/.config/meshift/secrets.env`). It only loads `.env` + `secrets.env` at startup, so a model change requires `sudo systemctl restart meshift.service` after editing `secrets.env`. The bridge calls `pi --mode rpc --provider <p> --model <m>`; provider/model pairs for `/model` cycling come from `PI_MODELS` (comma-separated `provider/model` entries). MiniMax provider + `MiniMax-M3` is already pre-configured in `/home/lepton/.pi/agent/auth.json` + `settings.json` (defaultProvider/Model), so the only edit needed to expose it via `/model` is appending `,minimax/MiniMax-M3` to `PI_MODELS`.
